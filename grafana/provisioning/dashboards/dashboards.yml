apiVersion: 1

providers:
  - name: 'Kafka Monitoring'
    orgId: 1
    folder: 'Kafka'
    folderUid: 'kafka'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
